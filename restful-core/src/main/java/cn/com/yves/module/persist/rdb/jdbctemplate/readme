持久层框架jdbcTemplate的运用.

条件过滤查询思路: 
    1.将满足条件的所有模型层的Id查询出来,然后通过内存数据根据Id获取对应的结构体.(内存里的数据结构只要一份前台需要的Bo,而不是维护POJO和Bo,这样减少内存空间和维护成本.)
    
    2.封装查询条件.
    
    3.关于高级查询时的设计思路是什么?
    
    
    
RDBValues: